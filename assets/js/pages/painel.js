import "../core/config.js";
const API=window.NC_CONFIG.apiBase;const token=localStorage.getItem("nc_token");const user=JSON.parse(localStorage.getItem("nc_user")||"null");const board=document.getElementById("painelBoard");const chips=Array.from(document.querySelectorAll(".filter-chip"));let filtro="TODAS";let cache=[];if(!token||!user){window.location.href="login.html"}async function api(path,options={}){const res=await fetch(API+path,{...options,headers:{"Content-Type":"application/json",Authorization:"Bearer "+token,...(options.headers||{})}});const data=await res.json().catch(()=>null);if(!res.ok)throw new Error(data&&data.error||"Erro na API");return data}function statusClass(status){const s=String(status||"").toUpperCase();if(s.includes("SETUP"))return"status-setup";if(s.includes("AJUSTE"))return"status-ajuste";if(s.includes("MANUT"))return"status-manutencao";if(s.includes("APOIO"))return"status-apoio";return""}function badgeClass(status){const s=String(status||"").toUpperCase();if(s.includes("SETUP"))return"setup";if(s.includes("AJUSTE"))return"ajuste";if(s.includes("MANUT"))return"manut";if(s.includes("APOIO"))return"apoio";return"obs"}function passaFiltro(item){if(filtro==="TODAS")return true;const s=String(item.status||"").toUpperCase();if(filtro==="MANUTENÇÃO")return s.includes("MANUT");if(filtro==="APOIO")return Number(item.precisa_apoio||0)===1;return s.includes(filtro)}function apoioTexto(item){if(!Number(item.precisa_apoio||0))return"Sem apoio";const st=String(item.apoio_status||"AGUARDANDO").replace("_"," ");return"Apoio: "+st}function card(item){const tnl=String(item.tnl||"").padStart(3,"0");const apoio=apoioTexto(item);return `<article class="machine-card ${statusClass(item.status)}"><div class="machine-head"><div class="machine-main"><div class="machine-token">${tnl}</div><div class="machine-name"><strong>TNL ${tnl}</strong><div class="machine-mini"><span>${item.status||"APOIO"}</span><span>${item.aberto_por||"Sistema"}</span></div></div></div><span class="badge ${badgeClass(item.status)}">${apoio}</span></div><div class="machine-body"><div class="machine-detail"><b>${item.motivo||"Sem motivo"}</b><br>${item.detalhe||item.observacao||"Sem descricao"}</div><div class="machine-actions"><button class="btn btn-ok" data-action="concluir" data-id="${item.id}">Concluir</button>${Number(item.precisa_apoio||0)?`<button class="btn btn-warn" data-action="assumir" data-id="${item.id}">Vou atender</button>`:""}</div></div></article>`}function render(){const items=cache.filter(passaFiltro);board.innerHTML=items.length?items.map(card).join(""):`<div class="empty-state">Nenhum card para esse filtro.</div>`}async function load(){board.innerHTML=`<div class="empty-state">Carregando painel...</div>`;try{const data=await api("/painel-geral");cache=data.ocorrencias||[];render()}catch(err){board.innerHTML=`<div class="empty-state">${err.message}</div>`}}chips.forEach(chip=>{chip.addEventListener("click",()=>{chips.forEach(c=>c.classList.remove("active"));chip.classList.add("active");filtro=chip.textContent.trim().toUpperCase();render()})});board.addEventListener("click",async event=>{const btn=event.target.closest("button[data-action]");if(!btn)return;btn.disabled=true;const id=btn.dataset.id;const action=btn.dataset.action;try{if(action==="concluir")await api("/concluir-ocorrencia",{method:"POST",body:JSON.stringify({id})});if(action==="assumir")await api("/assumir-ocorrencia",{method:"POST",body:JSON.stringify({id})});await load()}catch(err){alert(err.message)}finally{btn.disabled=false}});load();setInterval(load,15000);
